import React from 'react'
import CircularProgress from '@material-ui/core/CircularProgress'

const Loader = () => {
    return <CircularProgress size={'5vw'} thickness={2} />
}

export default Loader
